await function addChannel(name) {
    let twitchChannelURL = `https://www.twitch.tv/${name}`;
    if () {
        return -1;
    }
    else {
        if (isPartner()) {
            return 2;
        }
        else {
            return 1;
        }
    }
}

await function doesChannelExists(twitchChannelURL) {
    fetch(

    )
}

await function isPartner(twitchChannelURL) {
}
